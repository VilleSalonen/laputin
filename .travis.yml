language: node_js
node_js:
- '6'
- '5'
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
script: cd src && npm install && npm run test-server && cd client && npm install && cd .. && npm test-client